"""
AI 服务核心业务模块

========================================
📚 模块概述
========================================
本模块包含 AI 服务的所有核心业务逻辑，实现了完整的 RAG（检索增强生成）流水线。

RAG 流水线组件：
    embedding.py   → 文本向量化（将自然语言转换为数值向量）
    vector_store.py → 向量存储与检索（Qdrant 向量数据库操作）
    llm.py          → 大语言模型调用（OpenAI / Anthropic / Mock）
    retriever.py    → 检索器（组合向量检索与 LLM 生成）
    rag.py          → RAG 统一入口（对外暴露简洁的函数接口）

数据流向：

    用户问题
       │
       ▼
    embedding.py (文本 → 向量)
       │
       ▼
    vector_store.py (向量相似度搜索 → 相关文档)
       │
       ▼
    retriever.py (文档上下文 + 问题 → LLM)
       │
       ▼
    llm.py (生成最终回答)

💡 学习要点:
    1. 向量嵌入（Embedding）如何将语义信息编码为数学向量
    2. 余弦相似度如何衡量文本间的语义接近程度
    3. RAG 架构如何通过检索增强来减少 LLM 的"幻觉"
    4. 模块间的依赖关系与单向数据流设计
"""
