# F04 - AI智能问答 - 功能设计文档

## 📋 功能概述

AI智能问答是企业知识库问答系统的核心功能，通过RAG（检索增强生成）技术，结合大语言模型和向量检索，为用户提供准确、及时的智能问答服务。

## 🎯 用户故事

### 1. 智能问答
**作为** 知识库用户  
**我想要** 用自然语言提问  
**以便** 获得准确的答案和相关文档引用

**验收标准：**
- [ ] 支持自然语言提问
- [ ] AI理解问题意图
- [ ] 返回准确答案
- [ ] 提供答案来源引用
- [ ] 支持追问和澄清

### 2. 多轮对话
**作为** 知识库用户  
**我想要** 进行连续对话  
**以便** 获得更详细的信息

**验收标准：**
- [ ] 保存对话历史
- [ ] 理解上下文关系
- [ ] 支持问题追问
- [ ] 提供对话列表管理

### 3. 答案引用
**作为** 知识库用户  
**我想要** 查看答案来源  
**以便** 验证答案准确性

**验收标准：**
- [ ] 显示相关文档来源
- [ ] 标注文档匹配度
- [ ] 支持点击查看原文
- [ ] 显示答案引用片段

### 4. 智能推荐
**作为** 知识库用户  
**我想要** 获得相关问题推荐  
**以便** 发现更多有用信息

**验收标准：**
- [ ] 根据当前问题推荐相关问题
- [ ] 推荐问题可点击
- [ ] 推荐结果与上下文相关

## 🔐 功能需求

### 1. 对话界面

#### 功能描述
提供友好的对话界面，支持用户输入问题、显示AI回答、展示对话历史等。

#### 界面元素
- **消息列表**：显示用户和AI的对话内容
- **输入框**：支持多行输入、回车发送
- **发送按钮**：点击发送问题
- **对话历史**：左侧显示历史对话列表
- **欢迎消息**：AI首次回应的欢迎语

#### 消息类型

##### 用户消息
```
┌──────────────────────────────────┐
│                              [头像] │
│  如何申请年假？需要什么材料？  │
└──────────────────────────────────┘
```

##### AI消息
```
┌────────────────────────────────────────────┐
│ [头像] 智能助手                             │
│                                            │
│ 根据《员工手册》规定，年假申请流程如下：  │
│ 1. 提前15天提交申请                       │
│ 2. 通过OA系统填写《年假申请表》           │
│ 3. 直属领导审批                          │
│ 4. HR部门确认                            │
│                                            │
│ 📄 相关文档：                               │
│ 📄 员工请假管理办法 (PDF)                  │
│                                            │
│ 💡 相关问题：                               │
│ • 年假天数如何计算？                       │
│ • 年假可以跨年使用吗？                     │
└────────────────────────────────────────────┘
```

### 2. RAG检索流程

#### 检索增强生成流程
```
用户提问 → 意图识别 → 向量检索 → 文档召回 → 上下文构建 → LLM生成 → 答案返回
                ↓
         [知识库检索]
```

#### 流程详解

##### 1. 意图识别
- 分析用户问题类型
- 提取关键信息
- 判断是否需要检索

##### 2. 向量检索
- 将问题向量化
- 搜索相关文档向量
- 按相似度排序

##### 3. 文档召回
- 选择Top-K相关文档
- 提取文档内容
- 计算匹配度

##### 4. 上下文构建
- 组装问题+相关文档
- 添加系统提示词
- 构建完整prompt

##### 5. LLM生成
- 调用大语言模型
- 生成准确答案
- 引用文档来源

### 3. 对话管理

#### 对话状态
```typescript
interface Conversation {
  id: string              // 对话ID
  title: string           // 对话标题
  userId: number         // 用户ID
  messageCount: number    // 消息数量
  createdAt: Date        // 创建时间
  updatedAt: Date        // 更新时间
}
```

#### 消息类型
```typescript
interface Message {
  id: string              // 消息ID
  conversationId: string // 对话ID
  role: 'USER' | 'ASSISTANT'
  content: string         // 消息内容
  sources?: SourceDocument[] // 来源文档
  createdAt: Date        // 创建时间
}
```

#### 对话操作
- **新建对话**：创建新的对话会话
- **查看历史**：显示历史对话列表
- **删除对话**：删除对话及相关消息
- **清空历史**：清空某对话的所有消息
- **导出对话**：导出对话记录

### 4. 答案质量

#### 质量评估指标
- **准确性**：答案是否正确
- **完整性**：答案是否完整
- **相关性**：答案是否相关
- **可读性**：答案是否易于理解

#### 质量改进
- 用户反馈收集（点赞/点踩）
- 答案质量统计
- 持续优化提示词

## 🎨 界面设计

### 1. 对话主界面

#### 布局结构
```
┌────────────────────────────────────────────────┐
│  侧边栏      │        主对话区         │
│  ┌────────┐│   ┌──────────────────┐     │
│  │历史对话 ││   │  欢迎消息         │     │
│  │        ││   ├──────────────────┤     │
│  │新建对话 ││   │  用户问题         │     │
│  │        ││   ├──────────────────┤     │
│  │搜索   ││   │  AI回答          │     │
│  └────────┘│   ├──────────────────┤     │
│           │   │  追问问题         │     │
│           │   ├──────────────────┤     │
│           │   │  AI回答          │     │
│           │   └──────────────────┘     │
│           │        ┌──────────┐      │
│           │        │ 输入框  │      │
│           │        └──────────┘      │
└────────────────────────────────────────────────┘
```

#### 消息样式

##### 用户消息
- 背景：主题蓝色
- 对齐：右侧
- 圆角：12px
- 最大宽度：80%

##### AI消息
- 背景：白色
- 对齐：左侧
- 圆角：12px
- 包含：头像、答案、来源、推荐问题

### 2. 对话历史侧边栏

#### 功能区域
```
┌────────────────────────┐
│  新建对话  📝         │
├────────────────────────┤
│  搜索对话  🔍         │
├────────────────────────┤
│  历史对话             │
│  ┌───────────────┐   │
│  │产品发布流程    │   │
│  │年假申请流程    │   │
│  │报销政策咨询    │   │
│  └───────────────┘   │
└────────────────────────┘
```

#### 侧边栏功能
- **新建对话**：创建新对话
- **搜索对话**：搜索历史对话
- **对话列表**：显示历史对话
- **删除对话**：长按删除

### 3. 输入区域

#### 输入框功能
- **多行输入**：支持换行输入
- **快捷发送**：回车发送，Shift+Enter换行
- **字数限制**：500字符限制提示
- **输入提示**：显示可用的快捷操作

#### 快捷操作
- **清空输入**：清空输入框
- **语音输入**：支持语音输入（可选）
- **上传文件**：上传图片或文件（可选）

### 4. 来源文档展示

#### 来源卡片样式
```
┌────────────────────────────────────┐
│ 📄 产品发布标准流程 v2.0         │
│    产品管理 | 98% 匹配度           │
├────────────────────────────────────┤
│ 产品<b>发布</b>流程描述如下：     │
│ 1. 完成产品测试验收              │
│ 2. 准备发布材料...               │
│                                   │
│ [查看详情]                       │
└────────────────────────────────────┘
```

#### 来源信息
- **文档标题**：点击跳转到文档详情
- **匹配度**：显示相关度百分比
- **文档片段**：显示相关内容片段
- **查看详情**：链接到原文

## 🔗 接口定义

### 1. 提问接口

**接口地址：** `POST /api/v1/chat/ask`

**请求参数：**
```json
{
  "question": "如何申请年假？需要什么材料？",
  "conversationId": "optional-conversation-id"
}
```

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "conversationId": "conv-123",
    "answer": "根据《员工手册》规定，年假申请流程如下：\n\n1. 提前15天提交申请\n2. 通过OA系统填写《年假申请表》\n3. 直属领导审批\n4. HR部门确认",
    "sources": [
      {
        "documentId": 1,
        "title": "员工请假管理办法",
        "snippet": "员工申请年假需要提前15天提交申请",
        "relevance": 0.95,
        "url": "/documents/1"
      }
    ],
    "followUpQuestions": [
      "年假天数如何计算？",
      "年假可以跨年使用吗？"
    ]
  }
}
```

### 2. 获取对话列表

**接口地址：** `GET /api/v1/chat/conversations`

**请求参数：**
```json
{
  "page": 1,
  "pageSize": 20
}
```

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "items": [
      {
        "id": "conv-123",
        "title": "年假申请流程咨询",
        "messageCount": 5,
        "lastMessage": "年假天数如何计算？",
        "updatedAt": "2024-06-01T10:30:00Z"
      }
    ],
    "total": 23
  }
}
```

### 3. 获取对话详情

**接口地址：** `GET /api/v1/chat/conversations/{conversationId}`

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "id": "conv-123",
    "title": "年假申请流程咨询",
    "messages": [
      {
        "id": "msg-001",
        "role": "USER",
        "content": "如何申请年假？",
        "createdAt": "2024-06-01T10:00:00Z"
      },
      {
        "id": "msg-002",
        "role": "ASSISTANT",
        "content": "根据《员工手册》规定...",
        "sources": [...],
        "createdAt": "2024-06-01T10:00:05Z"
      }
    ]
  }
}
```

### 4. 删除对话

**接口地址：** `DELETE /api/v1/chat/conversations/{conversationId}`

**响应示例：**
```json
{
  "code": 200,
  "message": "对话已删除"
}
```

## 🤖 RAG系统设计

### 1. 检索增强生成架构

```
┌─────────────────────────────────────────────────┐
│              用户问题输入                         │
└────────────┬────────────────────────────────────┘
             ↓
┌─────────────────────────────────────────────────┐
│              意图理解和预处理                   │
│  • 提取关键信息                                  │
│  • 问题类型识别                                  │
│  • 候义词扩展                                    │
└────────────┬────────────────────────────────────┘
             ↓
┌─────────────────────────────────────────────────┐
│              向量检索                            │
│  • 问题向量化                                    │
│  • 相似度计算                                    │
│  • Top-K文档召回                                 │
└────────────┬────────────────────────────────────┘
             ↓
┌─────────────────────────────────────────────────┐
│              文档重排序                           │
│  • 二次相关性验证                                 │
│  • 多样性控制                                     │
└────────────┬────────────────────────────────────┘
             ↓
┌─────────────────────────────────────────────────┐
│              Prompt构建                         │
│  • 组装问题和文档                                 │
│  • 添加系统提示词                                 │
│  • 设置生成参数                                   │
└────────────┬────────────────────────────────────┘
             ↓
┌─────────────────────────────────────────────────┐
│              LLM生成                            │
│  • 调用大语言模型                                  │
│  • 生成答案                                       │
│  • 引用来源                                       │
└────────────┬────────────────────────────────────┘
             ↓
┌─────────────────────────────────────────────────┐
│              后处理                             │
│  • 答案格式化                                    │
│  • 来源链接生成                                  │
│  • 推荐问题生成                                  │
└─────────────────────────────────────────────────┘
```

### 2. 检索策略

#### 向量化模型
```python
# 使用Sentence-Transformers
from sentence_transformers import SentenceTransformer

model = SentenceTransformer('all-MiniLM-L6-v2')
embedding = model.encode("如何申请年假？")
```

#### 相似度计算
```python
# 使用余弦相似度
from sklearn.metrics.pairwise import cosine_similarity

similarity = cosine_similarity([query_embedding], [doc_embedding])
```

#### Top-K召回
```python
# 检索最相关的K个文档
TOP_K = 5
results = qdrant.search(
    collection_name="documents",
    query_vector=question_embedding,
    limit=TOP_K,
    score_threshold=0.7
)
```

### 3. Prompt工程

#### 系统提示词
```
你是一个专业的企业知识库助手，负责回答员工的问题。

你的任务：
1. 基于提供的文档内容回答问题
2. 如果文档中没有相关信息，明确告知用户
3. 保持答案简洁、准确、专业
4. 引用具体的文档来源

注意事项：
- 不要编造信息
- 如果不确定，建议用户查看相关文档
- 保持友好和专业的语气
```

#### 用户Prompt模板
```
问题：{question}

相关文档：
{document_1}
{document_2}
{document_3}

基于以上文档内容，请回答用户的问题。
```

### 4. 质量评估

#### 评估维度
- **准确率**：答案是否正确
- **完整性**：答案是否完整
- **相关性**：答案是否相关
- **可读性**：答案是否易于理解

#### 评估方式
- **用户反馈**：点赞/点踩
- **专家评审**：定期抽检答案质量
- **自动评估**：基于来源相关性

## 🧪 测试用例

### 1. 问答功能测试

#### 正常场景
- **TC-CHAT-001**: 提问简单问题，验证AI回答正确
- **TC-CHAT-002**: 提问复杂问题，验证AI回答详细
- **TC-CHAT-003**: 追问相关问题，验证上下文理解
- **TC-CHAT-004**: 验证来源文档显示正确

#### 异常场景
- **TC-CHAT-101**: 提问超出知识库范围的问题
- **TC-CHAT-102**: 问题表述不清，验证AI处理
- **TC-CHAT-103**: 验证AI拒绝回答不当问题

### 2. 性能测试

- **TC-PERF-001**: 问答响应时间测试
- **TC-PERF-002**: 并发问答测试
- **TC-PERF-003**: 长对话性能测试

## 📝 验收标准总结

### 功能完整性
- [ ] 支持自然语言问答
- [ ] 支持多轮对话
- [ ] 提供答案来源引用
- [ ] 支持对话历史管理
- [ ] 支持相关问题推荐

### 智能化水平
- [ ] 答案准确率 > 85%
- [ ] 响应时间 < 3秒
- [ ] 支持上下文理解
- [ ] 支持意图识别

### 用户体验
- [ ] 界面友好直观
- [ ] 回复及时响应
- [ ] 答案清晰准确
- [ ] 支持移动端访问

---

**文档版本：** v1.0  
**创建日期：** 2026-05-31  
**最后更新：** 2026-05-31  
**状态：** 待审核
